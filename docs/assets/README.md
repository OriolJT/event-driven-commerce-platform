# Screenshots

Replace the placeholder files in this directory with actual screenshots:

1. **grafana-dashboard.png** — Screenshot of the Platform Service Overview dashboard in Grafana
2. **tempo-trace.png** — Screenshot of a distributed trace in Grafana Tempo showing the saga flow

To capture:

1. Run the platform: `cd infra && docker compose up -d`
2. Create a few orders (see [demo.md](../demo.md))
3. Open Grafana at http://localhost:3000
4. Take screenshots of the dashboard and a trace view
5. Replace the placeholder files with the actual PNGs
